<!-- 새 슬래시 커맨드 템플릿. 복사해서 commands/<동사-목적>.md 로 쓰세요.
     커맨드는 "어떤 명령을 어떤 순서로 치나"만 다룹니다. 개념적 "왜"는 docs/ 로 위임(포인터만).
     작성 규칙의 근거: docs/_AUTHORING.md 부록 B. -->

# /<command-name> — <한 줄 목적>

<이 커맨드가 무엇을 하는지 1~2문장. 다른 sub-type/케이스로의 분기가 있으면 명시.>

근거(개념·계약): `docs/<관련문서>.md`. 본 커맨드는 절차만 다룹니다.

## 입력 인자

- `<arg>` — <의미> (기본값 / 필수 여부).
- `--<flag>` — <의미>.

## 절차

> step 번호(§N)는 다른 커맨드/문서가 인용하는 **안정 앵커**입니다. 재배치 금지, 새 step 은 append.

### §1 사전 점검
<저장소 루트 확인, 기존 산출물 충돌 확인(덮어쓰기 금지), 전제 조건.>

### §2 <생성/변경 작업>
<파일 생성/수정 단계. 어떤 파일을 어디에 만들고 무엇을 채우는지.>

### §3 검증
<클래스명/파일명 일치 등 정적 점검 + 실제 환경 검증은 `/verify` 위임 또는 인라인.>

### §4 보고
<무엇을 만들었고 어디에 있으며 다음 단계는 무엇인지.>

## 핵심 불변식 / 계약

- <어겨선 안 되는 규칙들 — 계약 파일 수정 금지, 이름 일치 규약, 부수효과 금지 등.>

## 검증 / 헬스 로직

| 증상 | 원인 | 조치 |
| --- | --- | --- |
| <로그/에러> | <원인> | <조치 또는 docs 포인터> |
